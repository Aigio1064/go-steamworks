# go-steamworks

SDK版本：164

Go语言的 Steamworks SDK 绑定

若要使用本库，请参照 https://partner.steamgames.com/doc/api

因为本人(Aigio1064)使用Windows系统作为开发主体，所以目前只有windows_amd64的版本，如果有需要，可以自行建立对应的loadLib.go。

开放 LIB 字段，可以通过此字段直接操作dylib